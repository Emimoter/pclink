<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.PClink" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.PClink.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_pclink_brand</item>
        <item name="postSplashScreenTheme">@style/Theme.PClink</item>
    </style>
</resources>
